# Phone Proof Required

Codex does not approve phone import.

After ChatGPT audits the exact XML and ZIP, a future confirmation-only phone test must prove:

1. the full project imports/renders;
2. Task 224 cannot run unless explicitly armed;
3. Task 224 calls Task 225 once;
4. no Send button, compose field, keyboard insertion, or paste action occurs;
5. the visible TextNow thread identity matches the bound sender;
6. the exact bound reply is read once and immediately followed by `Sent` in ordered output;
7. only the bound row changes from awaiting confirmation to DONE;
8. exact ID/DONE readback passes;
9. `%AIWConfirming` returns to 0;
10. Send/DONE duplication and Archive remain absent.

Gate 10 is not locked by this package. Archive, live/timer, capacity, and release remain blocked.
