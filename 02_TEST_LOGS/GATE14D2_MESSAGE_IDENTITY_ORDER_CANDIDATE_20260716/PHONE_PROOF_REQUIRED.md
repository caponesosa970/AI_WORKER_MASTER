# Phone Proof Required

Codex approves no import and claims no phone proof.

After ChatGPT audits the exact artifacts, two separate direct Sosa modes are required:

1. `ORDER_AND_LATER_REPEAT`: freshly stage exact synthetic rows 199-201, then prove ascending processing, three exact replies, repeated message acceptance under a new ID, three API calls, 3/3 lock accounting, and no lifecycle path.
2. `EXACT_DUPLICATE_ID`: preserve the staged IDs, then prove one exact existing ID is suppressed and one unseen control ID is eligible, with zero API calls, locks, or writes.

All profiles remain disabled. No real contact or phone number belongs in the dataset.
