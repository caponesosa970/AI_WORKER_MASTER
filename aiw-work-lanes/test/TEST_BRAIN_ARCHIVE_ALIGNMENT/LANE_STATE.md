# Test Lane — Brain Archive Alignment

Lane ID: `TEST_BRAIN_ARCHIVE_ALIGNMENT`

Classification: `ACTIVE / PHONE PROOF REQUIRED`

## Proven input

The three authorized D reads each returned 24 incorrect shifted rows instead of the 25 matching Archive rows. Every output row corresponded to the row immediately after a match, and the final match shifted to a blank row.

E and F are rejected and forbidden as evidence, parents, or phone-test artifacts.

## Current bounded test

The only authorized follow-up is the header-alignment diagnostic defined by the locked amended Brain-path plan. Production Brain/context construction remains unauthorized until the diagnostic returns correct aligned history.

## Promotion target

On phone PASS, promote only the proven AutoSheets Archive read contract into the Main Brain/context work. Do not promote the diagnostic task as production logic.
